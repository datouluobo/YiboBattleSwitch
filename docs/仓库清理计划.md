# 仓库清理计划

更新时间：`2026-07-05`

这份清单只列出当前仓库内已经确认属于构建产物、打包产物或空诊断目录的路径。按照仓库规则，这些路径在你明确确认之前不会被删除。

## 建议删除的路径

1. `E:\Program\YiboBattleSwitch\dist`
原因：TypeScript 与 Tailwind 的本地编译输出，可随时重新执行 `npm run build` 生成。

2. `E:\Program\YiboBattleSwitch\release`
原因：Electron Builder 打包产物，包含安装包、`win-unpacked` 目录和发布元数据，可随时重新执行 `npm run dist:win` 生成。

3. `E:\Program\YiboBattleSwitch\Logs`
原因：当前为空目录，不承载源代码或发布必需内容。

## 当前观察结果

- `dist/`：存在完整主进程、预加载和渲染进程编译结果。
- `release/`：存在 `YiboBattleSwitch-Setup-0.4.2.exe` 与 `win-unpacked/` 等大体积构建产物。
- `Logs/`：当前未发现文件。
- `node_modules/`：属于依赖目录，通常不提交，但这次未纳入默认删除清单，避免影响你当前本地开发环境。

## 建议的删除顺序

1. 删除 `dist/`
2. 删除 `release/`
3. 删除 `Logs/`

## 删除前确认文案

如果你要我继续执行删除，请直接回复：

`确认删除 dist、release、Logs`

我会只删除上面列出的明确路径，不做模糊匹配。
