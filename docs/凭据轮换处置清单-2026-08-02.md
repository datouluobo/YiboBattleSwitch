# 凭据轮换处置清单

发现日期：2026-08-02

## 已确认事实

候选目录 `vendor/nbb-core` 中存在未跟踪配置文件，包含第三方数据库、Redis 与日志服务连接凭据。该目录未进入 Git 历史，也未被当前 APPX/MSIX 打包。该目录已于 2026-08-02 在开发机手动删除；外部服务凭据仍必须轮换。

## 需要由对应服务管理员完成

1. 在数据库服务中轮换受影响账户密码；撤销旧账户或将其权限降至最小。
2. 在 Redis 服务中轮换访问密码并撤销旧密码。
3. 在日志/检索服务中轮换访问凭据、令牌和写入权限。
4. 检查上述服务的访问日志，重点查看 2026-06-29 至轮换完成期间的异常来源、批量读取和权限变更。
5. 检查开发机备份、同步盘、压缩包和聊天记录，确认旧配置未被复制；发现副本后按组织流程安全处置。
6. 不要把新凭据写入代码、仓库、日志或任何发布包；使用开发机本地机密存储或受控 CI 密钥。

## 完成记录

| 服务 | 轮换时间 | 执行人 | 旧凭据已撤销 | 日志审查完成 |
| --- | --- | --- | --- | --- |
| 数据库 | 待填写 | 待填写 | 待填写 | 待填写 |
| Redis | 待填写 | 待填写 | 待填写 | 待填写 |
| 日志服务 | 待填写 | 待填写 | 待填写 | 待填写 |
